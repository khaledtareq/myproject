on:
  push:
    branches:
      - master
      





jobs:
  build:
    runs-on: ubuntu-latest


    steps:
      - uses: actions/checkout@v3  


      - name: "login to docker hub"
        runs: docker login --username ${{ secrets.DOCKER_USERNAME }} --password ${{ secrets.DOCKER_PASSWOED }}


      - name: "building  docker image"
        runs:   docker build -t ${{ secrets.DOCKER_USERNAME }}/myimage .

      - name: "push to the docker hub"
        runs:  docker push ${{ secrets.DOCKER_USERNAME }}/myimage 

      