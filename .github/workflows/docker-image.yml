name: Docker Image CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    
    - name: Build and push the Docker image
       run: |
        echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "khaled2261" --password-stdin
        docker image build -t khaled:latest .
        docker push khaled:lates
 
